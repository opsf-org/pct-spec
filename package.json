{
  "name": "pct-spec",
  "private": true,
  "version": "0.1.0",
  "description": "Privacy Claims Token (PCT) — reference implementation",
  "workspaces": [
    "packages/core",
    "packages/api",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "Apache-2.0"
}
