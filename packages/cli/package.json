{
  "name": "@pct/cli",
  "version": "0.1.0",
  "description": "PCT command-line tool for issuing, verifying, and inspecting tokens",
  "type": "module",
  "bin": {
    "pct": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pct/core": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "license": "Apache-2.0"
}
